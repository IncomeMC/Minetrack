name: Record data point

on:
  schedule:
    - cron: '*/1 * * * *'
  workflow_dispatch:

jobs:
  record:
    runs-on: ubuntu-latest
    steps:
      - name: Ping /api/record
        run: curl -fsS --max-time 30 -o /dev/null "https://track.incomemc.net/api/record"